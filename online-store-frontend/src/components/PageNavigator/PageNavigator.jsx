import React from 'react'

export const PageNavigator = (props) => {
      const {product} = props

  return (
    <div className='pageNav'>
      {/* {product.category}
      {product.name} */}
    </div>
  )
}
